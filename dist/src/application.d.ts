import { Application, ApplicationConfig } from '@loopback/core';
export declare class MyApplication extends Application {
    options: ApplicationConfig;
    constructor(options?: ApplicationConfig);
}
